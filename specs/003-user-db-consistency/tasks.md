# Tasks: User Module Data-Integrity & Schema-Script Consistency

**Feature dir**: `specs/003-user-db-consistency/` · **Spec**: [spec.md](./spec.md)

**Tech**: .NET 8 / EF Core / SQL Server. Backend + DB artifact. Paths repo-relative.

**Audit source**: findings F1 (stale SQL schema artifact) and F2 (missing user-status CHECK
constraints). Frontend E-KYC handling verified correct — no frontend task required.

## Phase 1: Foundational

- [x] T001 Add `RoleConstants`-backed value list check: confirm the allowed `Role` set from `ProSport.Domain/Constants/Roles.cs` (source of truth for FR-005).

## Phase 2: User Story 2 — DB-enforced user status values (Priority: P2)

- [x] T002 [US2] In `ProSport.Infrastructure/Data/ProSportDbContext.cs`, add `CK_Users_EKycStatus` (`[EKycStatus] IN ('Unverified','Pending','Verified','Rejected')`) on the User entity mapping, using `EKycStatuses` constants (same shape as `CK_Matches_Status`).
- [x] T003 [US2] Add `CK_EkycProfiles_Status` (`[Status] IN ('Pending','Approved','Rejected')`) on the EkycProfile entity mapping.
- [x] T004 [US2] Add `CK_Users_Role` (`[Role] IN (...)`) on the User entity mapping, values from `Roles` constants.
- [x] T005 [US2] Generate EF migration `UserStatusConstraints` (`dotnet ef migrations add UserStatusConstraints -p ProSport.Infrastructure -s ProSport.API`); review the generated `AddCheckConstraint` calls.
- [x] T006 [US2] Add an xUnit `[SqlServerFact]` test `User_CheckConstraints_RejectInvalidValues` in `ProSport.Tests/SqlServerIntegrationTests.cs` (insert invalid EKycStatus / Role / EkycProfile.Status → expect `DbUpdateException`), parallel to the existing Voucher/Tournament constraint tests.

**Checkpoint**: invalid user-status values are rejected at the DB layer.

## Phase 3: User Story 1 — Trustworthy schema artifact (Priority: P1)

- [x] T007 [US1] Regenerate `src/database/ProSportDB_EFCore_AutoGenerated.sql` from the current migrations (e.g. `dotnet ef migrations script -i -o ../../database/ProSportDB_EFCore_AutoGenerated.sql -p ProSport.Infrastructure -s ProSport.API`), so it reflects InitialCreate + TournamentStatusConstraint + UserStatusConstraints and contains all CK constraints.
- [x] T008 [US1] Verify the regenerated script contains every status CHECK constraint (`CK_Bookings_Status`, `CK_Matches_Status`, `CK_Vouchers_Status`, `CK_Tournaments_Status`, and the new `CK_Users_*` / `CK_EkycProfiles_Status`).
- [x] T009 [US1] Update `src/database/obsolete/README.md` (and any doc pointing at the artifact) so the described regeneration command matches what actually produces the committed file (FR-007).

**Checkpoint**: the committed SQL artifact matches the migrations-produced schema.

## Phase 4: Polish

- [x] T010 Apply the migration to the dev DB (`dotnet ef database update`) and run `dotnet build` + `dotnet test ProSport.sln` — 0 errors, existing suite green (SC-003).
- [ ] T011 (optional, skipped — harmless cosmetic) Remove the unused `NotSubmitted` label from `src/frontend/src/pages/admin/AdminUsersPage.jsx` EKYC maps (cosmetic; not a bug).

---

## Dependencies & order
- T001 → T002–T004 (need the role set) → T005 (migration) → T006 (test).
- US2 (constraints, T002–T006) MUST land before US1 T007 so the regenerated script includes the new constraints.
- T010 last.

## Implementation strategy
- **MVP**: US2 (DB constraints) + US1 T007 regeneration — closes both real findings.
- Total: **11 tasks** (Foundational 1, US2 5, US1 3, Polish 2). No frontend correctness work needed.
