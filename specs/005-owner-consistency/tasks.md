# Tasks: Owner Frontend & Database Consistency

**Feature dir**: `specs/005-owner-consistency/` · **Spec**: [spec.md](./spec.md)

**Tech**: React (Vite) FE + .NET 8 / EF Core / SQL Server. Paths repo-relative.

## Phase 1: User Story 1 — Fix broken membership toggle (Priority: P1) 🎯 MVP

- [x] T001 [US1] In `src/frontend/src/pages/owner/OwnerMembershipsPage.jsx`, change `toggleStatus` (line ~73) so deactivation sends a backend-valid status — `next = m.status === 'Active' ? 'Cancelled' : 'Active'` — instead of the invalid `'Suspended'`. Update the button labels/badge accordingly ("Hủy" / "Kích hoạt").
- [x] T002 [US1] Verify against `MembershipService.AllowedStatuses` ({Active, Expired, Cancelled}) that the toggle now round-trips; extend/adjust `OwnerMembershipsPage` test if one exists.

**Checkpoint**: owner can deactivate/reactivate a membership without a 400.

## Phase 2: User Story 2 — Owner bookings full state coverage (Priority: P2)

- [x] T003 [US2] In `src/frontend/src/pages/owner/OwnerBookingsPage.jsx`, add `Expired` and `NoShow` to `STATUS_OPTIONS`; give all options Vietnamese labels (consistent with AdminBookingsPage). Confirm `OwnerStatusBadge type="booking"` renders these states (extend its map if needed in `src/frontend/src/components/owner/index.jsx`).
- [x] T004 [US2] (low) Add `Expired`/`NoShow` handling to `OwnerBookingCalendarPage.jsx` where booking statuses are colored/filtered.

**Checkpoint**: every booking state is filterable & labelled in owner views.

## Phase 3: User Story 3 — Owner-managed status integrity (Priority: P3)

> ✅ **RESOLVED — status sets confirmed by the user, then implemented.** Chosen (evidence-based,
> superset of existing data so the migration applies cleanly):
> **Equipment** = {Available, Discontinued}; **Complex** / **ComplexOwner** = {Active, Inactive};
> **ComplexReview** = {Published, Hidden}. Migration `OwnerEntityStatusConstraints` applied to dev DB.
> (T011 StaffAssignment `Suspended` is a separate UI/diagram decision, left open.)

- [x] T005 [US3] In `EquipmentService`, validate `dto.Status` against an allowed set {Active, Inactive} before `equipment.Status = dto.Status` (return 400 on invalid), mirroring `OwnerStaffService`/`MembershipService`.
- [x] T006 [US3] In `ProSport.Infrastructure/Data/ProSportDbContext.cs`, add `CK_Equipments_Status` (`[Status] IN ('Active','Inactive')`) — confirm the exact table name (Equipment/Equipments) from the model first.
- [x] T007 [US3] (FR-005, low) Add `CK_Complexes_Status`, `CK_ComplexOwners_Status` (`[Status] IN ('Active','Inactive')`) and `CK_ComplexReviews_Status` (allowed set incl. `Published`) — confirm allowed values from constants/defaults first.
- [x] T008 [US3] Generate EF migration `OwnerEntityStatusConstraints`; review generated `AddCheckConstraint` calls.
- [x] T009 [US3] Add `[SqlServerFact]` test `Equipment_CheckConstraint_RejectsInvalidStatus` in `ProSport.Tests/SqlServerIntegrationTests.cs`.

## Phase 4: Polish

- [x] T010 Apply migration to dev DB; regenerate `src/database/ProSportDB_EFCore_AutoGenerated.sql`; run `dotnet build` + `dotnet test ProSport.sln` (0 regressions) and the frontend owner tests (`npx vitest run src/pages/owner`).
- [ ] T011 (FR-006, decision — still open) Resolve `StaffAssignment.Suspended`: either add it to `OwnerStaffPage` toggle/options, or remove it from `OwnerStaffService` allowed set — and reflect the choice in the Staff state diagram.

---

## Dependencies & order
- **US1 (T001–T002)** is the MVP — independent frontend-only fix of the broken toggle.
- **US2 (T003–T004)** frontend-only, independent.
- **US3 (T005–T009)** backend/DB; T005 (service validation) before/with T006 (constraint); T008 after all CK added.
- **Polish (T010)** after US3 so the regenerated script includes new constraints. T011 is a standalone decision.

## Implementation strategy
- **MVP = US1** (fix the broken "Tạm ngưng" button — the only real functional bug).
- Then US2 (booking filter coverage), then US3 (Equipment + optional Complex/Review DB integrity).
- Total: **11 tasks** (US1: 2, US2: 2, US3: 5, Polish: 2).
