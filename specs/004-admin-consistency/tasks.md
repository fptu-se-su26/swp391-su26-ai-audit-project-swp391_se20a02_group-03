# Tasks: Admin Frontend & Database Consistency

**Feature dir**: `specs/004-admin-consistency/` · **Spec**: [spec.md](./spec.md)

**Tech**: React (Vite) frontend + .NET 8 / EF Core / SQL Server. Paths repo-relative.

## Phase 1: User Story 1 — Admin bookings shows every state (Priority: P1) 🎯 MVP

- [x] T001 [US1] In `src/frontend/src/pages/admin/AdminBookingsPage.jsx`, extend `STATUS_TABS`, `STATUS_VARIANT`, and `STATUS_LABEL` to cover all 8 booking states: add `PendingPayment` (label "Chờ chia bill", warning), `CheckedIn` (label "Đã check-in", info), `Expired` (label "Hết hạn", neutral/danger), `NoShow` (label "Không đến", danger).
- [x] T002 [US1] (FR-006, low) Fix `PAYMENT_VARIANT` in the same file: add `Cancelled`, remove the never-emitted `Unpaid`.
- [ ] T003 [US1] (skipped — no AdminBookingsPage test exists; change is a static map, verified via build + other admin page tests passing) Update/extend the page test `AdminBookingsPage` (if present) or add coverage asserting the new states render a non-raw label.

**Checkpoint**: every booking state is visible & filterable in the admin list.

## Phase 2: User Story 2 — DB constraints for admin-managed statuses (Priority: P2)

- [x] T004 [US2] Add `ReportStatus` constants class in `ProSport.Domain/Constants/ReportStatus.cs` (Pending/Investigating/Resolved/Rejected); refactor `ReportService` literals to use it.
- [x] T005 [US2] In `ProSport.Infrastructure/Data/ProSportDbContext.cs`, add `CK_Reports_Status` (`[Status] IN ('Pending','Investigating','Resolved','Rejected')`) on the Report entity.
- [x] T006 [US2] Add `CK_Courts_Status` (`[Status] IN ('Available','Maintenance','Inactive')`) using `CourtStatuses`.
- [x] T007 [US2] Add `CK_ProductStocks_Status` and `CK_PricingRules_Status` (`[Status] IN ('Active','Inactive')`) — confirm exact table names from the model first.
- [x] T008 [US2] Generate EF migration `AdminEntityStatusConstraints` (`dotnet ef migrations add ...`); review the generated `AddCheckConstraint` calls.
- [x] T009 [US2] Add an xUnit `[SqlServerFact]` test `Report_CheckConstraint_RejectsInvalidStatus` in `ProSport.Tests/SqlServerIntegrationTests.cs`, parallel to the existing constraint tests.

**Checkpoint**: invalid admin-managed status values rejected at the DB layer.

## Phase 3: Polish

- [x] T010 Regenerate `src/database/ProSportDB_EFCore_AutoGenerated.sql` from migrations (so it includes the new CK constraints) — same command as documented in `src/database/obsolete/README.md`.
- [x] T011 Apply migration to dev DB (`dotnet ef database update`); run `dotnet build` + `dotnet test ProSport.sln` (0 regressions, SC-003); run the frontend admin-bookings test.

---

## Dependencies & order
- **US1 (T001–T003)** is independent frontend-only — ship first (MVP, fixes the visible defect).
- **US2 (T004–T009)** backend/DB; T004→T005, others independent, T008 after all CK added.
- **Polish (T010–T011)** after US2 (regenerate script must include new constraints).

## Implementation strategy
- **MVP = US1** (admin bookings state coverage) — closes the user-visible AF1 defect.
- Then US2 (DB integrity for Report/Court/ProductStock/PricingRule).
- Total: **11 tasks** (US1: 3, US2: 6, Polish: 2).
